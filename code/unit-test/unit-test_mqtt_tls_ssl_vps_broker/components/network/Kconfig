menu "Network Configuration"
    config ESP_WIFI_SSID
        string "WiFi SSID"
        default "myssid"
        help
            SSID (network name) 

    config ESP_WIFI_PASSWORD
        string "WiFi Password"
        default "mypassword"
        help
            WiFi password (WPA or WPA2) 

    config MQTT_URI
        string "MQTT URI path"
        default ""
        help
            The URI path for the MQTT to connect to

endmenu
